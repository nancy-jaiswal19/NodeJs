// Import the fs core nodule

const fs = require('fs')

const sourceFile = './Unit 1/notes/StreamModuleData.txt'
const destinationFile = 'copied_source.txt'

console.log(`Starting effiencu to copy of ${sourceFile} to ${destinationFile} `);


